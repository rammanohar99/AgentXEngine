"""
packages.shared — shared utilities used across packages.

Public surface:
    from packages.shared.utils import (
        generate_id, truncate_text, content_hash,
        clean_whitespace, extract_code_blocks, count_tokens_approx
    )
"""
