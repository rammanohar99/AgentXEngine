"""
packages.rag — RAG pipeline.

Lazy imports only — importable without database or Vertex AI installed.

Public surface:
    from packages.rag.chunker import Chunker
    from packages.rag.embeddings import EmbeddingService
    from packages.rag.schemas import Document, Chunk, RetrievalResult, ...
"""
